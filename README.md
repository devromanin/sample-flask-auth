# sample-flask-auth

Uma API de autenticação básica em Python + Flask com suporte a banco de dados. Serve como ponto de partida pra qualquer projeto que precise de login, logout e gerenciamento de usuários.
